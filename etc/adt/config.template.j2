# ADT deployment descriptor - generated by adt.sh deploy
# Do not edit by hand
INSTANCE_STATUS=deployed

# Instance identity
INSTANCE_KEY={{ INSTANCE_KEY }}
COMPOSE_PROJECT={{ COMPOSE_PROJECT }}
PRODUCT_NAME={{ PRODUCT_NAME }}
PRODUCT_VERSION={{ PRODUCT_VERSION }}
INSTANCE_ID={{ INSTANCE_ID }}

# Images
IMAGE={{ IMAGE }}
IMAGE_TAG={{ IMAGE_TAG }}
DB_IMAGE={{ DB_IMAGE }}
ES_IMAGE={{ ES_IMAGE }}

# Database
DEPLOYMENT_DB_TYPE={{ DEPLOYMENT_DB_TYPE }}
DEPLOYMENT_DB_NAME={{ DEPLOYMENT_DB_NAME }}
DEPLOYMENT_DB_USER={{ DEPLOYMENT_DB_USER }}

# Networking
DEPLOYMENT_EXT_HOST={{ DEPLOYMENT_EXT_HOST }}
ACCEPTANCE_SCHEME={{ ACCEPTANCE_SCHEME }}
ACCEPTANCE_HOST={{ ACCEPTANCE_HOST }}
DEPLOYMENT_FRONTEND_NETWORK={{ DEPLOYMENT_FRONTEND_NETWORK }}

# TLS
INSTANCE_DOMAIN={{ INSTANCE_DOMAIN }}
INSTANCE_SSL_CERTIFICATE_FILE={{ INSTANCE_SSL_CERTIFICATE_FILE }}
INSTANCE_SSL_CERTIFICATE_KEY_FILE={{ INSTANCE_SSL_CERTIFICATE_KEY_FILE }}
INSTANCE_SSL_CERTIFICATE_CHAIN_FILE={{ INSTANCE_SSL_CERTIFICATE_CHAIN_FILE }}

# JVM
DEPLOYMENT_JVM_SIZE_MAX={{ DEPLOYMENT_JVM_SIZE_MAX }}
DEPLOYMENT_JVM_SIZE_MIN={{ DEPLOYMENT_JVM_SIZE_MIN }}

# Features
DEPLOYMENT_ONLYOFFICE_ENABLED={{ DEPLOYMENT_ONLYOFFICE_ENABLED }}
DEPLOYMENT_MAILPIT_ENABLED={{ DEPLOYMENT_MAILPIT_ENABLED }}
DEPLOYMENT_MATRIX_ENABLED={{ DEPLOYMENT_MATRIX_ENABLED }}
DEPLOYMENT_JITSI_ENABLED={{ DEPLOYMENT_JITSI_ENABLED }}
DEPLOYMENT_AI_ENABLED={{ DEPLOYMENT_AI_ENABLED }}
DEPLOYMENT_IFRAMELY_ENABLED={{ DEPLOYMENT_IFRAMELY_ENABLED }}
DEPLOYMENT_CLOUDBEAVER_ENABLED={{ DEPLOYMENT_CLOUDBEAVER_ENABLED }}
DEPLOYMENT_KEYCLOAK_ENABLED={{ DEPLOYMENT_KEYCLOAK_ENABLED }}
DEPLOYMENT_LDAP_ENABLED={{ DEPLOYMENT_LDAP_ENABLED }}
DEPLOYMENT_CALDAV_ENABLED={{ DEPLOYMENT_CALDAV_ENABLED }}
DEPLOYMENT_CLAMAV_ENABLED={{ DEPLOYMENT_CLAMAV_ENABLED }}
DEPLOYMENT_FRONTAIL_ENABLED={{ DEPLOYMENT_FRONTAIL_ENABLED }}
DEPLOYMENT_DOZZLE_ENABLED={{ DEPLOYMENT_DOZZLE_ENABLED }}

# Add-ons / labels
DEPLOYMENT_ADDONS={{ DEPLOYMENT_ADDONS }}
DEPLOYMENT_LABELS={{ DEPLOYMENT_LABELS }}

# Paths
PROJECT_DIR={{ PROJECTS_DIR }}/{{ INSTANCE_KEY }}
ADT_DATA={{ ADT_DATA }}
